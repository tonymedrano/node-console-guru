# node-console-guru (ES6)
This basic node spoiler for quick code test in the console

# run
npm install && npm start. 
# If you don't have nodemon (npm i -g nodemon), then run directly as: 
node ./app.js
